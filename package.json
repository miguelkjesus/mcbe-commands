{
  "name": "@mhesus/mcbe-commands",
  "version": "1.0.1",
  "description": "A library that implements a simple JSON-based database into minecraft bedrock utilising dynamic properties.",
  "author": "Miguel Jesus",
  "license": "ISC",
  "main": "build/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepack": "npm ci && npm run build",
    "build": "rimraf dist && tsc"
  },
  "devDependencies": {
    "@minecraft/server": "^1.9.0-beta.1.20.60-preview.23",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
